export async function POST() {
  return Response.json({
    ok: true,
    message: "Worker withdrawal API placeholder. Current MVP creates localStorage withdraw_request from the worker wallet page.",
  });
}
