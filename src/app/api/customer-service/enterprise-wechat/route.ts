export async function GET() {
  return Response.json({
    ok: true,
    message: "Enterprise WeChat customer service placeholder. Current MVP reads configured links from shared settings and does not call real API.",
  });
}
